NULL =
SUBDIRS = gee src tests

AM_CPPFLAGS = \
		$(NULL)

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = gdom3-1.0.pc

EXTRA_DIST = \
	gdom3-1.0.pc.in \
	Makefile.decl \
	$(NULL)

DISTCLEANFILES = \
	$(NULL)

